services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - DB_PATH=/app/data/tasks.sqlite
    volumes:
      - ./data:/app/data
    mem_limit: 512m
